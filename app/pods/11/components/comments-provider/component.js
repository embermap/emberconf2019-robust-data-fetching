import Component from '@ember/component';

export default Component.extend({
  // this will be passed into the component
  article: null,

  // Have this component provide this articles comments
  // comments: [?????]
});
